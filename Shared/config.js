/* ============================================================
   Shared/config.js
   مكان واحد بس لعنوان السيرفر (الباك إند) — أي صفحة في المنصة
   بتاخد العنوان من هنا بدل ما يتكرر مكتوب جوه كل ملف لوحده.

   لو هتغيّر استضافة الباك إند (دبلويمنت جديد) كل اللي محتاجه إنك
   تغيّر السطر بتاع API_ROOT هنا بس، وكل الصفحات هتتحدث تلقائيًا.
   ============================================================ */

(function () {
  // عنوان جذر السيرفر (من غير /api ومن غير / في الآخر)
  const API_ROOT = "https://softprime-001-site1.ftempurl.com";

  window.APP_CONFIG = {
    // جذر السيرفر (يستخدم لبناء روابط الفيديوهات مثلًا)
    ROOT: API_ROOT,
    // جذر الـ API (بادئة كل نداءات الباك إند)
    API_BASE: API_ROOT + "/api",
  };
})();
